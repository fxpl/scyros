void add(){

    int linha = IDX;
    // if(value == 0)
    //     return;

    // while(A[i][linha]!= NULL)
    //     linha++;
    // A[i][linha] = (Matriz*) malloc(sizeof(Matriz));
    // A[i][linha]->j=j;
    // A[i][linha]->ele = value;

}

